import { OrdinalSpanishPipe } from './ordinal-spanish.pipe';

describe('OrdinalSpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdinalSpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
